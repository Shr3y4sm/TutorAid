export interface ClassItem {
  id: string;
  subject: string;
  teacher: string;
  time: string;
  room: string;
}

export interface AnnouncementItem {
  id: string;
  title: string;
  description: string;
  date: string;
}